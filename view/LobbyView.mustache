<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Lobby</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #e6f5c9;
        }
        .score-circle {
            background: white;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            box-shadow: 2px 2px 5px gray;
            margin-left: 10px;
        }
        .game-box {
            background-color: #b2d8b2;
            border: 2px solid green;
            border-radius: 10px;
            height: 50px;
        }
        .rounded-btn {
            border-radius: 30px;
            background: #2a34bf;
            padding: 10px 30px;
            font-weight: bold;
            color: white;
            border: none;
        }
        .logout-btn {
            background-color: #ff4d4d;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: bold;
        }
    </style>
</head>
<body>

<div class="container-fluid mt-3">
    <div class="d-flex justify-content-between align-items-center">
        <h4 class="ms-3"><strong>Logo</strong></h4>
        <form method="post" action="/TPFinal/login/logOut">
            <button class="logout-btn">Cerrar sesión</button>
        </form>
    </div>
</div>

<div class="container text-center mt-4">

    <div class="d-flex justify-content-center align-items-center mb-2">
        <h1 class="fw-bold text-primary">{{usuario}}</h1>
        <div class="score-circle">123</div>
    </div>

    <p class="fw-bold">Últimas partidas</p>

    <div class="mx-auto mb-2 game-box w-75 mt-4"></div>
    <div class="mx-auto mb-2 game-box w-75 mt-4"></div>
    <div class="mx-auto mb-2 game-box w-75 mt-4"></div>
    <div class="mx-auto mb-4 game-box w-75 mt-4"></div>

    <div class="d-flex justify-content-between px-5">
        <button class="rounded-btn">Iniciar partida</button>
        <button class="rounded-btn">Crear pregunta</button>
    </div>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>